const num: number = 42;

let username: string | number = 'Alice';

username = 'Bob';
username = 7;

// new things

// username = true;

const numbers: (number | string)[] = [];

numbers.push(1);
numbers.push('Dean');
// numbers.push(true);

export default numbers;
